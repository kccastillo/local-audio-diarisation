# Diarizer dependencies — verified clean install on Windows 11 + RTX 3070 + CUDA 12.x.
# Stack chosen 2026-05-04 (faster-whisper + pyannote.audio 3.3 over WhisperX).
#
# IMPORTANT — torch must be installed FIRST from the PyTorch CUDA wheel index, NOT PyPI.
# A bare `pip install -r requirements.txt` would pull CPU-only torch from PyPI on Windows.
# Two-step install:
#
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.5.1+cu121 torchaudio==2.5.1+cu121
#   pip install -r requirements.txt
#
# (Lines below assume torch + torchaudio are already installed via the index-url above.)

ctranslate2==4.5.0          # cuDNN 9-compatible; matches torch 2.5.1+cu121
faster-whisper==1.2.0       # no torch pin (BYO torch); CTranslate2-backed Whisper
pyannote.audio==3.3.2       # NOT 4.x — 4.0.2+ pins torch==2.8.0 exactly (pyannote-audio#1976)
huggingface_hub<1.0         # 1.x removed `use_auth_token` kwarg; pyannote 3.3.2 still uses it
speechbrain==1.0.3          # 1.1.0 added a k2 lazy-import that breaks pyannote on Windows
matplotlib                  # untracked transitive of pyannote.audio 3.3.2 (not auto-pulled)
soundfile>=0.13             # WAV/FLAC I/O
librosa>=0.11               # audio analysis (used by inspect script + measurement gate)
numpy>=2.0
pyyaml>=6.0
pytest>=8.0                 # dev only
pytest-mock>=3.14           # dev only
fastapi>=0.110              # webapp backend
uvicorn>=0.30               # webapp server (used by `diarizer serve`)
httpx>=0.27                 # async test client + dev only
